<UserControl x:Class="LibrarianDashboard.Views.IssuedBooksView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1000">
    <Grid>
         <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
         </Grid.RowDefinitions>
         <StackPanel Grid.Row="0" Margin="0,0,0,20">
             <!-- Back buttonLogic handled by Tab switching in main window mostly, but we can keep it if needed -->
             <TextBlock Text="Currently Issued Books" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource DeepText}"/>
             <TextBlock Text="Track who has what, and when it is due." Foreground="{DynamicResource TextSecondaryBrush}" FontSize="13" Margin="0,5,0,0"/>
         </StackPanel>
         <Border Grid.Row="1" Background="White" CornerRadius="15" Padding="5">
             <DataGrid ItemsSource="{Binding FilteredIssuedBooks}" AutoGenerateColumns="False" IsReadOnly="True" Margin="10">
                 <DataGrid.Columns>
                     <DataGridTextColumn Header="Book Title" Binding="{Binding Title}" Width="1.5*"/>
                     <DataGridTextColumn Header="Borrowed By" Binding="{Binding BorrowedBy}" Width="1.2*"/>
                     <DataGridTextColumn Header="Due Date" Binding="{Binding DueDateDisplay, Mode=OneWay}" Width="*"/>
                     
                     <DataGridTextColumn Header="Fine" Binding="{Binding FineDisplay, Mode=OneWay}" Width="0.8*" FontWeight="Bold" Foreground="#FF4081"/>

                     <DataGridTemplateColumn Header="Status" Width="*">
                         <DataGridTemplateColumn.CellTemplate>
                             <DataTemplate>
                                 <Border Background="#FFF0F0" CornerRadius="5" Padding="10,5" HorizontalAlignment="Left">
                                     <TextBlock Text="{Binding Status}" Foreground="#FF4081" FontWeight="Bold" FontSize="11"/>
                                 </Border>
                             </DataTemplate>
                         </DataGridTemplateColumn.CellTemplate>
                     </DataGridTemplateColumn>

                     <DataGridTemplateColumn Header="Return" Width="100">
                          <DataGridTemplateColumn.CellTemplate>
                             <DataTemplate>
                                 <Button Click="BtnAction_Click" Content="Return" 
                                         Background="{DynamicResource HighlightBrush}" Foreground="White" 
                                         FontSize="11" FontWeight="Bold" Padding="10,5" 
                                         BorderThickness="0" Cursor="Hand">
                                     <Button.Template>
                                         <ControlTemplate TargetType="Button">
                                             <Border Background="{TemplateBinding Background}" CornerRadius="5">
                                                 <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                             </Border>
                                         </ControlTemplate>
                                     </Button.Template>
                                 </Button>
                             </DataTemplate>
                         </DataGridTemplateColumn.CellTemplate>
                     </DataGridTemplateColumn>
                 </DataGrid.Columns>
             </DataGrid>
         </Border>
    </Grid>
</UserControl>
