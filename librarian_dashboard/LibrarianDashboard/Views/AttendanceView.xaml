<UserControl x:Class="LibrarianDashboard.Views.AttendanceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:local="clr-namespace:LibrarianDashboard"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1000">
    <Grid>
        <ScrollViewer>
            <StackPanel>
                <TextBlock Text="Student Attendance" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource DeepText}" Margin="0,0,0,20"/>

                <!-- Today's Attendance Summary -->
                <Border Background="White" CornerRadius="15" Padding="20" Margin="0,0,0,20">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                            <TextBlock Text="Present Today" FontSize="14" Foreground="#666" Margin="0,0,0,5"/>
                            <TextBlock Text="{Binding TodaysAttendance.Count}" FontSize="28" FontWeight="Bold" Foreground="#4CAF50"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="Currently In Library" FontSize="14" Foreground="#666" Margin="0,0,0,5"/>
                            <TextBlock Text="{Binding TodaysAttendance, Converter={StaticResource CountActiveAttendanceConverter}}" FontSize="28" FontWeight="Bold" Foreground="#DC2626"/>
                        </StackPanel>

                        <!-- Total Hours removed for simplicity as it requires complex converter -->
                    </Grid>
                </Border>

                <!-- Quick Entry/Exit Actions -->
                <Border Background="White" CornerRadius="15" Padding="20" Margin="0,0,0,20">
                    <Grid>
                        <TextBlock Text="Quick Actions" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource DeepText}" Margin="0,0,0,15"/>
                        <ItemsControl ItemsSource="{Binding AllMembers}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#F8FAFC" CornerRadius="10" Padding="15" Margin="0,0,0,10">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="14" Foreground="{DynamicResource DeepText}"/>
                                                <TextBlock Text="{Binding RollNumber}" FontSize="12" Foreground="#666" Margin="0,2,0,0"/>
                                            </StackPanel>

                                            <StackPanel Grid.Column="1" Orientation="Horizontal">
                                                <Button Click="BtnMarkEntry_Click" Content="📥 Entry" 
                                                        Background="#4CAF50" Foreground="White" BorderThickness="0" 
                                                        Padding="12,6" FontSize="12" FontWeight="SemiBold" Cursor="Hand" Margin="0,0,8,0">
                                                    <Button.Template>
                                                        <ControlTemplate TargetType="Button">
                                                            <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                            </Border>
                                                        </ControlTemplate>
                                                    </Button.Template>
                                                </Button>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Grid>
                </Border>

                <!-- Attendance Log -->
                <Border Background="White" CornerRadius="15" Padding="20">
                    <Grid>
                        <TextBlock Text="Today's Attendance Log" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource DeepText}" Margin="0,0,0,15"/>
                        <ItemsControl ItemsSource="{Binding TodaysAttendance}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#F8FAFC" CornerRadius="8" Padding="15" Margin="0,0,0,8">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding StudentName}" FontWeight="SemiBold" FontSize="14" Foreground="{DynamicResource DeepText}"/>
                                                <TextBlock Text="{Binding RollNumber}" FontSize="12" Foreground="#666"/>
                                            </StackPanel>

                                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="20,0">
                                                <TextBlock Text="Entry:" FontSize="11" Foreground="#666"/>
                                                <TextBlock Text="{Binding EntryTimeDisplay}" FontSize="13" FontWeight="SemiBold" Foreground="#4CAF50"/>
                                            </StackPanel>

                                            <StackPanel Grid.Column="2" VerticalAlignment="Center" Margin="20,0">
                                                <TextBlock Text="Exit:" FontSize="11" Foreground="#666"/>
                                                <TextBlock Text="{Binding ExitTimeDisplay}" FontSize="13" FontWeight="SemiBold" Foreground="#F44336"/>
                                            </StackPanel>

                                            <StackPanel Grid.Column="3" VerticalAlignment="Center" Margin="20,0">
                                                <TextBlock Text="Duration:" FontSize="11" Foreground="#666"/>
                                                <TextBlock Text="{Binding Duration}" FontSize="13" FontWeight="SemiBold" Foreground="#FF9800"/>
                                                <!-- Mark Exit Button -->
                                                <Button Click="BtnMarkExit_Click" Content="Mark Exit" 
                                                        Background="#F44336" Foreground="White" BorderThickness="0" 
                                                        Padding="8,4" FontSize="11" FontWeight="SemiBold" Cursor="Hand" Margin="0,5,0,0"
                                                        Visibility="{Binding ExitTime, Converter={StaticResource NullToVisibilityConverter}}">
                                                    <Button.Template>
                                                        <ControlTemplate TargetType="Button">
                                                            <Border Background="{TemplateBinding Background}" CornerRadius="4" Padding="{TemplateBinding Padding}">
                                                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                            </Border>
                                                        </ControlTemplate>
                                                    </Button.Template>
                                                </Button>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Grid>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
