<UserControl x:Class="LibrarianDashboard.Views.BooksView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1000">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- TOP BAR -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="Book Inventory" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource DeepText}"/>
                <TextBlock Text="Manage library collection and catalog." Foreground="{DynamicResource TextSecondaryBrush}" FontSize="13" Margin="0,5,0,0"/>

                <!-- Category Chips -->
                <StackPanel Orientation="Horizontal" Margin="0,20,0,0">
                    <RadioButton Content="All Books" Style="{StaticResource FilterChip}" IsChecked="True"/>
                    <RadioButton Content="Technology" Style="{StaticResource FilterChip}"/>
                    <RadioButton Content="Fiction" Style="{StaticResource FilterChip}"/>
                    <RadioButton Content="Science" Style="{StaticResource FilterChip}"/>
                    <RadioButton Content="History" Style="{StaticResource FilterChip}"/>
                </StackPanel>
            </StackPanel>

            <StackPanel Grid.Column="1" VerticalAlignment="Top">
                 <Button Click="BtnOpenAddBook_Click" Background="{DynamicResource HighlightBrush}" Foreground="White" Padding="20,10" BorderThickness="0" FontSize="14" FontWeight="SemiBold">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="12" Padding="{TemplateBinding Padding}" Cursor="Hand">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                    <Path Data="{StaticResource IconPlus}" Fill="White" Width="12" Height="12" Margin="0,0,8,0"/>
                                    <TextBlock Text="Add New Book" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </StackPanel>
        </Grid>

        <!-- DATA GRID -->
        <Border Grid.Row="1" Background="{DynamicResource CardBg}" CornerRadius="15" Padding="5">
            <DataGrid ItemsSource="{Binding FilteredBooks}" AutoGenerateColumns="False" IsReadOnly="True" Margin="10">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Book Title" Binding="{Binding Title}" Width="1.5*"/>
                    <DataGridTextColumn Header="Author" Binding="{Binding Author}" Width="*"/>
                    <DataGridTextColumn Header="Shelf" Binding="{Binding ShelfLocation}" Width="0.5*"/>
                    <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="0.8*"/>
                    
                    <DataGridTemplateColumn Header="Status" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Background="#EEF2FF" CornerRadius="5" Padding="10,5" HorizontalAlignment="Left">
                                    <TextBlock Text="{Binding Status}" Foreground="{DynamicResource HighlightBrush}" FontWeight="Bold" FontSize="11"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                    <DataGridTemplateColumn Header="Actions" Width="200">
                          <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <!-- Issue / Return -->
                                    <Button Click="BtnAction_Click" Content="{Binding ActionLabel}" 
                                            Background="{Binding ActionColor}" Foreground="White" 
                                            FontSize="10" FontWeight="Bold" Padding="12,6" 
                                            BorderThickness="0" Cursor="Hand" Margin="0,0,5,0">
                                        <Button.Template>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}" CornerRadius="5">
                                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Button.Template>
                                    </Button>

                                    <!-- Edit -->
                                    <Button Click="BtnEditBook_Click" Background="Transparent" BorderThickness="0" Cursor="Hand" ToolTip="Edit Book" Margin="5,0">
                                        <Path Data="{StaticResource IconEdit}" Fill="#8E94A4" Width="14" Height="14" Stretch="Uniform"/>
                                    </Button>

                                    <!-- Delete -->
                                    <Button Click="BtnDeleteBook_Click" Background="Transparent" BorderThickness="0" Cursor="Hand" ToolTip="Delete Book">
                                        <Path Data="{StaticResource IconDelete}" Fill="#FF4081" Width="14" Height="14" Stretch="Uniform"/>
                                    </Button>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
