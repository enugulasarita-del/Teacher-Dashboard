<UserControl x:Class="LibrarianDashboard.Views.NoticesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1000">
    <Grid>
         <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1.5*"/> <!-- Active Notices -->
            <ColumnDefinition Width="*"/>   <!-- Post New -->
         </Grid.ColumnDefinitions>

         <StackPanel Grid.Column="0" Margin="0,0,20,0">
             <TextBlock Text="Campus Notices" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource DeepText}" Margin="0,0,0,20"/>

             <ScrollViewer VerticalScrollBarVisibility="Hidden">
                 <ItemsControl ItemsSource="{Binding AllNotices}">
                     <ItemsControl.ItemTemplate>
                         <DataTemplate>
                             <Border Background="{DynamicResource CardBg}" CornerRadius="12" Padding="20" Margin="0,0,0,15" BorderBrush="{DynamicResource CardBorderBrush}" BorderThickness="1">
                                 <Grid>
                                     <Grid.RowDefinitions>
                                         <RowDefinition Height="Auto"/>
                                         <RowDefinition Height="Auto"/>
                                         <RowDefinition Height="Auto"/>
                                     </Grid.RowDefinitions>
                                     
                                     <Grid Grid.Row="0" Margin="0,0,0,10">
                                         <WrapPanel>
                                             <Border Background="#FEF2F2" CornerRadius="4" Padding="8,4" Margin="0,0,10,0" Visibility="{Binding TargetDept, Converter={StaticResource NullToVisibilityConverter}}">
                                                 <TextBlock Text="{Binding TargetDept}" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource HighlightBrush}"/>
                                             </Border>
                                             <Border Background="#F0F9FF" CornerRadius="4" Padding="8,4" Margin="0,0,10,0" Visibility="{Binding TargetYear, Converter={StaticResource NullToVisibilityConverter}}">
                                                 <TextBlock Text="{Binding TargetYearDisplay}" FontSize="11" FontWeight="Bold" Foreground="#0284C7"/>
                                             </Border>
                                             <TextBlock Text="{Binding DateDisplay}" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" VerticalAlignment="Center"/>
                                         </WrapPanel>
                                         <Button Content="🗑" Click="BtnDeleteNotice_Click" HorizontalAlignment="Right" Background="Transparent" BorderThickness="0" Foreground="#F43F5E"/>
                                     </Grid>

                                     <TextBlock Grid.Row="1" Text="{Binding Title}" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource DeepText}" Margin="0,0,0,8"/>
                                     <TextBlock Grid.Row="2" Text="{Binding Message}" FontSize="13" Foreground="{DynamicResource TextSecondaryBrush}" TextWrapping="Wrap" LineHeight="20"/>
                                 </Grid>
                             </Border>
                         </DataTemplate>
                     </ItemsControl.ItemTemplate>
                 </ItemsControl>
             </ScrollViewer>
         </StackPanel>

         <!-- POST FORM -->
         <Border Grid.Column="1" Background="{DynamicResource CardBg}" CornerRadius="16" Padding="25" VerticalAlignment="Top" BorderThickness="1" BorderBrush="{DynamicResource CardBorderBrush}">
             <StackPanel>
                 <TextBlock Text="Post New Notice" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource DeepText}" Margin="0,0,0,20"/>
                 
                 <TextBlock Text="Notice Title" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,5"/>
                 <TextBox x:Name="TxtNoticeTitle" Height="40" Padding="10" FontSize="14" Margin="0,0,0,15"/>

                 <TextBlock Text="Message Content" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,5"/>
                 <TextBox x:Name="TxtNoticeBody" Height="100" Padding="10" FontSize="14" TextWrapping="Wrap" Margin="0,0,0,15"/>

                 <TextBlock Text="Target Department (Optional)" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,5"/>
                 <ComboBox x:Name="CmbNoticeDept" Height="40" Padding="10" Margin="0,0,0,15">
                     <ComboBoxItem Content="All Departments"/>
                     <ComboBoxItem Content="Computer Science (CS)"/>
                     <ComboBoxItem Content="Information Technology (IT)"/>
                     <ComboBoxItem Content="Electronics (ECE)"/>
                 </ComboBox>

                 <TextBlock Text="Target Year (Optional)" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,5"/>
                 <ComboBox x:Name="CmbNoticeYear" Height="40" Padding="10" Margin="0,0,0,25">
                     <ComboBoxItem Content="All Years"/>
                     <ComboBoxItem Content="Year 1"/>
                     <ComboBoxItem Content="Year 2"/>
                     <ComboBoxItem Content="Year 3"/>
                     <ComboBoxItem Content="Year 4"/>
                 </ComboBox>

                 <Button Click="BtnPostNotice_Click" Content="Publish Notice" 
                         Background="{DynamicResource HighlightBrush}" Foreground="White" Height="45" FontSize="14" FontWeight="Bold">
                     <Button.Template>
                         <ControlTemplate TargetType="Button">
                             <Border Background="{TemplateBinding Background}" CornerRadius="8" Cursor="Hand">
                                 <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                             </Border>
                         </ControlTemplate>
                     </Button.Template>
                 </Button>
             </StackPanel>
         </Border>
    </Grid>
</UserControl>
